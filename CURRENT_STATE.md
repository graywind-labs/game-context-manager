# Game Context Manager 当前开发状态

更新时间：2026-06-25

## 1. 当前阶段

T037 Ctrl+S 保存与自动目录导出已完成。当前代码在 T036 导出完成提示去重基础上，节点详情可用 `Ctrl+S` 触发保存，保存或删除节点、文件、图片后会自动同步目录索引，创建主节点后会自动导出 AGENTS/CLAUDE：

- 当前主界面保持左侧节点/文件树 + 右侧详情区结构。
- API 配置、语言切换、退出账号只在设置中管理。
- AI 默认可用；API 未配置、配置不完整或生成失败时，在用户触发 AI 功能时提示不可用或错误。
- 当前代码和前端文案表已清理旧“节点树 / 快捷操作 / Assistant panel / Local game context workspace”等旧式主界面文案。
- 创建节点弹窗移除了右上角重复取消按钮，仅保留底部取消操作。
- 设置弹窗顶部只保留一个“设置”标题，右上角关闭设置改为关闭图标按钮。
- 设置语言下拉选项固定显示为 `简体中文` / `English`；选择语言后需要点击确认才切换界面语言，未变更时确认按钮禁用。
- 设置中的退出账号按钮已移动到 API 配置下方，当前登录用户显示在退出账号按钮上方。
- 节点详情中的保存/删除按钮文案已精简为“保存”“删除”；游戏、模块、内容详情中按 `Ctrl+S` 等同点击保存按钮，未修改或保存按钮不可用时无反应。
- 节点详情中的 AI 增加、修改、润色和汇总入口已移动到基础信息下方的“AI辅助编辑”分组框，按钮文案精简为“增加”“修改”“润色”“汇总”，汇总按钮改为填充色。
- 节点详情中的 MD 预览改为弹窗显示，不再展开在详情页底部。
- AI 辅助编辑弹窗右上角关闭按钮已改为与设置弹窗一致的关闭图标。
- 左下角导出和设置操作区已固定在主界面左下角，不随右侧详情滚动或左侧树滚动移出视野。
- 应用默认标题和中文界面左上角产品名已改为“游戏上下文管理器”，语言切换时浏览器文档/窗口标题同步切换；Electron 默认 File/Edit/View/Window/Help 菜单已隐藏。
- 左下角设置按钮右侧已增加齿轮图标。
- 节点详情底部程序生成信息框已去掉“结构字段”标题，创建者和最后编辑者改为显示本地用户名。
- 图片详情页和图片库卡片不再展示原始文件名或生成路径，图片详情页底部显示该图片关联的节点列表，便于双向反查。
- 左侧图片库栏位不再显示图片数量。
- 节点详情点击保存或在有改动时按 `Ctrl+S` 后会显示约 2 秒的保存成功或保存失败提示，避免手动编辑保存后无反馈。
- 左下角导出成功或失败提示改为约 2 秒后自动消失。
- 左下角两个导出按钮完成或失败时，只在左下角导出区显示“导出完成 / 导出失败”短暂提示，不再额外触发右下角全局导出提示；保存、刷新、删除等其它流程的右下角全局提示保持不变。
- 图片详情中的删除图片按钮文案已精简为“删除”。
- Agent 文件和索引文件详情页已加入文件内容预览，通过 main process 安全读取当前工作区内已知文件。
- 模块节点和内容节点详情字段标题、AI 辅助字段名、导出 Markdown 标题和设计文档中的“（主观）”括注已移除；旧 Markdown 导入仍兼容带“（主观）”的历史标题。
- 游戏、模块、内容节点详情的选填输入框不再反复显示“可留空，MD 中会保留标题方便后续补充。”占位文案。
- 内容节点底部程序生成信息已在模块 ID 右侧增加“所属模块”，显示父模块名称。
- 节点详情保存按钮已改为显式触发异步保存；`corepack pnpm start` 启动的是 `out/` 已构建产物，不需要单独打开后端。开发时如需立即看到源码改动，应使用 `corepack pnpm dev`，或先执行 `corepack pnpm build` 再执行 `corepack pnpm start`。
- 节点、图片和已知导出文件保存或删除成功后会自动导出当前目录索引；删除确认弹窗不再提示用户手动导出目录。
- 左侧树中已创建的模块节点、图片库、Agent 文件分组和索引文件分组已加入折叠/展开按钮；没有子项时按钮禁用为灰色。
- 左侧树中已创建的主节点也已加入折叠/展开按钮，可折叠下方模块、内容节点和图片库；Agent 文件和索引文件不随主节点折叠，只由各自分组折叠按钮独立控制。创建子节点的 `+` 按钮改为青色填充白色图标，以区别于折叠状态的 `+`。
- 左侧树内容节点支持直接点击选中；即使当前选中的是主节点，也会自动切换到该内容节点所属模块并显示内容详情。
- 模块节点和内容节点详情的关联截图区域已改为横向卡片排列，多个图片超出宽度时通过底部横向滚动条拖拽查看。
- 创建主节点时会自动导出 `AGENTS.md`、`CLAUDE.md`；左下角仍可手动重新导出 AGENTS/CLAUDE。自动或手动导出的 `AGENTS.md`、`CLAUDE.md`、`INDEX.md`、`manifest.yml` 和 `image_catalog.yml` 已跟随设置中的语言导出；中文设置下人类可读正文、标题和说明为中文，文件名、路径和 YAML 机器字段名保持英文以便 Agent 稳定解析。
- 手动导出的 `AGENTS.md` 面向 Codex/通用 Agent，`CLAUDE.md` 面向 Claude Code / WorkBuddy 等 Claude 系 Agent；两者都是同等完整的任务手册，只在首段身份说明和必要措辞上适配不同 Agent，并都包含任务定位、读取顺序、上下文选择、图片/UI 读取、维度型字段查询、常见任务处理方式、竞品/市场/用户分析、依据标注、推测、未知信息处理规则和边界。
- 手动导出的 `INDEX.md` 已加入“如何使用本索引 / How To Use This Index”和三层节点结构化字段地图，说明游戏主节点、模块节点、内容/阶段/体验节点分别有哪些字段、字段位置和适用场景。
- 手动导出的 `manifest.yml` 已加入机器可读 `field_schema`、`task_context_entries`、`workspace.agents`、`workspace.claude` 和 `game.image_catalog`，便于下游 Agent 直接发现入口和字段含义。
- 手动导出的 `image_catalog.yml` 在保留 `linked_nodes` 的基础上新增 `linked_node_files`，可按图片反查关联节点类型、ID、名称和路径；游戏主图也会纳入图片反查关系。
- 示例工作区 `examples/sample-workspace/` 已用当前导出服务重新生成 Agent 文件、节点 Markdown、`manifest.yml`、`INDEX.md` 和 `image_catalog.yml`。
- `workspaceService.ts` 中未使用的旧版下游 Agent 文档常量已删除，避免后续误用旧模板。
- 创建节点弹窗和上传图片弹窗已支持按空格键触发创建/上传主操作；焦点位于输入框、文本域、下拉框或按钮内时保留原生空格行为。
- 点击左上角导入时，如果当前已打开工作区，界面会保持当前工作区直到用户选择文件夹且导入成功；取消选择或导入失败不会退出当前工作区，导入失败会显示短暂错误提示。
- 模块和内容详情的关联截图横向滚动已约束在“关联截图”栏位内部，不再撑出详情页边框或触发主界面横向滚动条。
- 模块节点详情中的关联截图栏位已移动到“AI辅助编辑”分组框下方、模块定位字段上方。
- 游戏主节点的“补充说明”字段已从前端表单、共享类型、SQLite 读写映射、Markdown 导出、工作区导入解析和 AI 可编辑字段中移除；历史数据库中的旧 `notes` 列保留但不再作为游戏主节点字段暴露。
- 右侧详情页已移除固定窄版最大宽度限制，会随主窗口拉宽或最大化自适应扩展，文本填充框和关联图片栏位可使用更多横向空间。
- Agent 文件和目录索引文件详情页右上角已加入“删除”按钮，删除前使用应用内确认弹窗；main process 只允许删除当前工作区内程序识别的 `AGENTS.md`、`CLAUDE.md`、`manifest.yml`、`INDEX.md`、`image_catalog.yml`。
- 左侧文件、图片和节点条目右键菜单底部已加入“删除”选项；文件走已知导出文件删除流程，节点和图片复用各自详情页的删除确认、权限校验和删除逻辑。
- Agent 文件和目录索引文件详情页已移除无用的“文件路径”栏位，只保留文件内容预览和删除入口。
- 设置界面已在 API 配置下方、当前用户上方加入“帮助”按钮；点击后会以与 Agent/索引文件预览一致的弹窗方式显示应用根目录 `README.md`。
- 创建模块、创建内容和上传图片弹窗的示例占位文案已调整为更贴近实际游戏上下文；创建模块/内容弹窗左上角冗余分类字已移除，上传图片弹窗左上角“图片库”和右上角重复取消按钮已移除。
- 新建主节点时，主节点目录名已改为“主节点名称 + 游戏上下文”后再清洗；例如游戏名“使命防线”会生成 `使命防线游戏上下文/`，后续 `game.md`、模块、内容、图片、`INDEX.md` 和 `image_catalog.yml` 都位于该目录下。
- 创建游戏主节点流程已改为每次都重新选择工作区根目录，不再默认复用当前或上次选择的路径；所选文件夹是 Agent 运行根目录，主节点游戏上下文文件夹作为该根目录下的子文件夹生成。取消选择或创建失败时，已打开的旧工作区保持不变。
- 删除游戏主节点现在会删除该主节点的整个游戏上下文文件夹，并同步删除工作区根目录的 `.game-context-manager.yml`，同时清除主节点、模块、内容和图片资产数据库记录；删除模块会删除对应模块文件夹及其内容文件。递归删除前会校验目标目录必须位于当前工作区内部，marker 删除前也会校验目标文件必须位于当前工作区内部，避免误删工作区外路径。
- 应用重启恢复最近工作区时，如果该工作区的 `.game-context-manager.yml` 已不存在，会跳过恢复并回到未打开工作区状态，避免删除主节点后重启仍尝试导入已失效工作区。
- 主界面右侧顶部已加入刷新按钮（旋转箭头图标）；点击后会重新扫描当前工作区磁盘文件，并用扫描结果替换本地数据库快照和刷新左侧目录。用户在系统资源管理器中删除、增加或修改工作区内节点/图片/索引文件后，可通过刷新按钮对齐管理器显示；应用重启后会自动恢复最近工作区并以磁盘重新扫描结果为准。
- 修复模块节点和内容节点详情页删除按钮会把点击事件误传为待删除节点对象的问题；现在详情页删除模块/内容时会正确使用当前选中节点 ID，不再触发 SQLite 参数绑定错误。
- 左下角导出/设置区域的短暂提示已移动到导出按钮上方；导出和设置按钮保持在左下角固定位置，不会因提示出现而被顶起或改变位置。
- 修复刷新或重启恢复工作区后左侧索引文件只剩 `manifest.yml` 的问题；磁盘扫描摘要现在会把游戏目录内已有的 `INDEX.md` 和 `image_catalog.yml` 一起加入文件列表。
- 刷新或重启恢复工作区时，如果旧 `manifest.yml` 仍引用已经被用户从资源管理器删除的模块、内容或游戏 Markdown 文件，导入逻辑会记录 warning 并回退扫描工作区中实际存在的 Markdown 文件，不再抛出 `Referenced file is missing`。
- 刷新或重启恢复工作区时，如果旧 `manifest.yml` / `image_catalog.yml` 仍引用已删除图片文件，导入逻辑会跳过缺失图片并记录 warning。
- 根据实际导入到的游戏、模块和内容节点过滤旧 `manifest.yml` 的图片 `linked_nodes`，避免过期索引把已经不存在的模块或内容图片关联重新写回数据库。
- 过期索引 warning 会把工作区目录索引标记为需要重新导出；应用内保存或删除节点、文件、图片后会自动更新 `manifest.yml`、`INDEX.md`、`image_catalog.yml`，用户也可点击“导出当前目录”手动重导出。
- 左侧索引文件列表中 `manifest.yml`、`INDEX.md` 和 `image_catalog.yml` 已统一为同一缩进层级显示。
- 工作区创建/导入摘要类型与 `workspaceService` 已移除旧 `USAGE.md`、工作区 README、`games/` 字段和死模板。
- 示例工作区 `examples/sample-workspace/` 已迁移到新版 marker-based 结构。
- 示例工作区根目录包含 `.game-context-manager.yml`、`AGENTS.md`、`CLAUDE.md`、`manifest.yml`，主节点目录为 `starfall_workshop/`。
- 示例模块和内容采用目标路径：`modules/<module_id>/module.md` 与 `modules/<module_id>/contents/<content_id>.md`。
- 示例 `manifest.yml`、`INDEX.md`、`image_catalog.yml`、示例 README、下游 `AGENTS.md` / `CLAUDE.md` 均已更新为新版路径和读取说明，不再依赖 `USAGE.md`。
- 根 `README.md` 已更新为当前 T030 后状态，移除旧 MVP / 待迁移说明，并补充新版示例结构。
- `sampleWorkspace.test.ts` 覆盖新版示例 marker、manifest 路径、模块/内容嵌套路径、示例可被导入服务读取，以及示例文件不包含 API key。

## 2. 当前产品定位

Game Context Manager 保持本地优先定位，只负责帮助人类管理、规整、查看、编辑、删除、汇总和导出结构化游戏上下文，让其他 Agent 能够读取普通文件形态的上下文源。

它不负责数据分析、生图、自动运营建议、RAG、MCP、自动玩游戏、联网公共项目空间或云端多人协作。

## 3. 当前技术决策

已落地：

- Electron
- React
- TypeScript
- Vite
- Tailwind CSS
- pnpm / Corepack
- SQLite（当前使用 Electron/Node 内置 `node:sqlite`，schema version 5）
- Node 文件系统 API
- Markdown + YAML frontmatter 节点文件
- `.game-context-manager.yml` 工作区唯一标记
- 新版根目录工作区结构和主节点文件夹路径
- 游戏、模块、内容节点与图片多对多关联
- 节点和图片顺序 ID：`game_001`、`module_001`、`content_001`、`image_001`
- 创建主节点时自动导出、也可手动重导出 `AGENTS.md` / `CLAUDE.md`
- 保存或删除后自动导出、也可手动重导出 `manifest.yml` / `INDEX.md` / `image_catalog.yml`
- 下游 Agent 完整任务手册式 `AGENTS.md` / `CLAUDE.md`
- `INDEX.md` 人类可读结构化字段地图
- `manifest.yml` 机器可读 `field_schema`
- `image_catalog.yml` 图片到关联节点文件的 `linked_node_files` 反查
- 工作区目录索引过期状态 `directory_index_needs_export`
- 索引缺失时从节点 Markdown 与图片目录导入重建
- OpenAI-compatible / mock AI Provider 抽象
- 本地 API 配置保存与连接测试（无 AI 启用开关）
- 设置模型 `app_settings` 与 UI 语言偏好保存
- 应用级本地登录门槛与退出账号
- 两栏主界面壳、空工作区灰色占位树、左下角导出/设置入口
- 字段级 AI 增加/修改/润色弹窗，多字段选择、结果对比、编辑和确认覆盖
- 三级内容到二级模块、二级模块到一级游戏的 AI 汇总弹窗和确认覆盖
- 左侧图片库上传弹窗、图片预览小图、左侧拖拽、弹窗拖拽和剪贴板图片粘贴
- 删除权限、应用内确认弹窗、模块级联内容删除、图片引用清理和左侧右键“在文件夹中打开”
- 新版示例工作区和 Agent 可读性测试

## 4. 当前文件状态

当前主要代码结构：

```text
src/
  main/
    index.ts
    ipc/
      apiIpc.ts
      contentIpc.ts
      exportIpc.ts
      gameIpc.ts
      imageIpc.ts
      moduleIpc.ts
      settingsIpc.ts
      userIpc.ts
      workspaceIpc.ts
    services/
      aiProviderService.ts
      exportWorkflowService.ts
      fileExportService.ts
      sqliteService.ts
      workspaceService.ts
  preload/
    index.ts
  renderer/
    index.html
    src/
      main.tsx
      App.tsx
      styles/
        index.css
  shared/
    index.ts
    constants/
      fields.ts
    types/
      api.ts
      content.ts
      domain.ts
      export.ts
      game.ts
      image.ts
      module.ts
      settings.ts
      user.ts
      workspace.ts
```

生成工作区的当前实际结构：

```text
<selected-folder>/
  .game-context-manager.yml
  AGENTS.md                          # 创建主节点时自动导出，也可手动重导出
  CLAUDE.md                          # 创建主节点时自动导出，也可手动重导出
  manifest.yml                       # 保存或删除后自动导出当前目录，包含 field_schema

  <game_folder_name>/                 # 以“主节点名称 + 游戏上下文”清洗后生成，例如：使命防线游戏上下文
    game.md                          # 节点保存时写入
    INDEX.md                         # 保存或删除后自动导出当前目录，包含结构化字段地图
    image_catalog.yml                # 保存或删除后自动导出当前目录，包含 linked_node_files 图片反查
    assets/
      images/
        <image_id>__<display_name>.<ext>
    modules/
      <module_id>/
        module.md                    # 节点保存时写入
        contents/
          <content_id>.md            # 节点保存时写入
```

当前示例工作区结构：

```text
examples/sample-workspace/
  .game-context-manager.yml
  AGENTS.md
  CLAUDE.md
  manifest.yml
  README.md
  starfall_workshop/
    game.md
    INDEX.md
    image_catalog.yml
    assets/images/
    modules/core_loop/module.md
    modules/core_loop/contents/day1_lobby_first_loop.md
    modules/upgrade_shop/module.md
    modules/upgrade_shop/contents/day2_upgrade_pressure.md
```

## 5. 已完成任务

```text
T001 — 项目脚手架初始化
T002 — 领域模型与 TypeScript 类型定义
T003 — 本地数据库与迁移初始化
T004 — 工作空间创建与基础文件结构生成
T005 — 基础 GUI 布局与节点树壳
T006 — 本地用户注册/选择
T007 — 游戏主节点创建、查看、编辑
T008 — 图片上传、重命名、图库管理
T009 — 模块节点 CRUD 与图片关联
T010 — 三级内容节点 CRUD、账号状态字段与 @ 图片引用
T011 — 节点删除与引用安全
T012 — MD 预览与 GUI 组合显示
T013 — API 配置页与 AI Provider 抽象
T014 — AI 字段级增加/修改/润色
T015 — 自下而上汇总：三级到二级
T016 — 自下而上汇总：二级到一级
T017 — 从已有 game-context 文件夹重建索引
T018 — UI 打磨：类 OpenWebUI + 类 XMind 节点管理
T019 — 示例工作空间与 Agent 可读性验证
T020 — 打包与 MVP 收尾
T021 — 新版工作区标记、文件结构与导入基础
T022 — 手动 Agent 文件导出与目录索引导出
T023 — 登录门槛、设置模型与 API 启用逻辑清理
T024 — 两栏主界面壳与空工作区节点树
T025 — 节点创建弹窗、层级加号与自动选中
T026 — 节点详情页、脏状态保存、图片关联与 MD 预览
T027 — 左侧图片库、上传弹窗、拖拽与剪贴板粘贴
T028 — 删除权限、确认弹窗与左侧右键菜单
T029 — 节点详情内 AI 编辑弹窗与父节点汇总
T030 — 语言与旧 UI 清理、示例迁移和完整烟测
T031 — 下游 Agent 指令与目录索引重构
T032 — AGENTS 与 CLAUDE 指令完整度对齐
T033 — 左侧主节点折叠边界修正
T034 — 刷新工作区时忽略过期索引中的缺失节点
T035 — 删除主节点时同步删除工作区唯一标记
T036 — 导出完成提示去重
T037 — Ctrl+S 保存与自动目录导出
```

## 6. 当前 TODO

`TASK.md` 中当前没有剩余 TODO。

T037 已作为用户明确指定的保存与导出工作流调整完成并记录。后续如继续开发，应先新增或明确新的 `TASK.md` 任务，再按 `AGENTS.md` 工作流执行。

## 7. 最近一次测试结果

本次运行过的命令与结果：

```text
corepack pnpm typecheck
通过。执行 tsconfig.node.json 与 tsconfig.web.json 类型检查。

corepack pnpm test
通过。执行 tsconfig.test.json 类型检查，以及 SQLite、工作区、文件导出、自动导出工作流、AI provider 和 sample workspace 单元测试。新增 `exportWorkflowService.test.ts` 覆盖自动导出 Agent 文件、目录索引，并清除 `directoryIndexNeedsExport` 状态。测试过程中 node:sqlite 会输出 ExperimentalWarning，但命令成功。

corepack pnpm lint
失败。当前 package.json 尚未配置 lint 脚本，pnpm 报告 Command "lint" not found。

corepack pnpm build
通过。执行类型检查和 electron-vite build，输出 out/main、out/preload、out/renderer。

git diff --check
通过。仅输出当前工作区 LF/CRLF 转换提示，没有空白错误。
```

## 8. GUI 人工烟测记录

本次未在真实桌面窗口中执行人工点击烟测；当前环境完成了命令级可用检查（typecheck/test/build/diff-check）。以下为 T030 完整 GUI 人工烟测清单，需在可交互桌面窗口中执行：

1. 启动应用后，无最近登录用户时出现本地用户登录/创建弹窗，主体不可操作。
2. 创建本地用户后进入应用；通过设置退出账号后，再次出现登录弹窗。
3. 在设置中选择中文但未点击确认时，界面语言不变且确认按钮可点击；点击确认后，主界面、弹窗、按钮、错误提示均为中文，不出现英文并列文案。
4. 在设置中选择英文但未点击确认时，界面语言不变且确认按钮可点击；点击确认后，主界面、弹窗、按钮、错误提示均为英文，不出现中文并列文案；语言下拉选项始终显示为 `简体中文` / `English`。
5. 主界面只有左侧节点/文件树和右侧详情区，没有旧顶部主状态栏、旧三栏布局或旧右侧常驻辅助面板。
6. 未打开工作区时，左侧显示灰色创建主节点、创建模块节点、创建内容节点占位，右侧提示选择节点。
7. 左上角导入缺失 marker 的目录时失败；导入包含多个 marker 的目录时失败；导入新版示例工作区时成功。
8. 从灰色主节点 `+` 创建主节点和工作区，创建弹窗不要求用户输入节点 ID，右上角没有重复取消按钮；点击创建后必须选择工作区根目录，创建后自动选中主节点。
9. 主节点 `+` 创建模块，模块 `+` 创建内容；创建后左侧树刷新并自动选中新节点。
10. 游戏、模块、内容详情页未修改时保存按钮禁用，按 `Ctrl+S` 无反应；修改字段后保存按钮启用，点击保存或按 `Ctrl+S` 后显示约 2 秒的成功或失败提示，保存后回到禁用状态。
11. 节点详情中的图片关联使用图片名和预览图选择，不出现手填图片 ID 或普通复选框列表。
12. 图片库在左侧与模块同级显示，不显示图片数量，支持按钮上传、左侧拖拽上传、上传弹窗拖拽和剪贴板粘贴。
13. 点击图片条目后，右侧只显示图片详情和原图，不与节点详情混排。
14. 每个节点详情可通过“MD 预览”按钮打开弹窗查看 Markdown 内容，关闭弹窗后回到节点详情。
15. AI 增加、修改、润色均从节点基础信息下方的“AI辅助编辑”分组框进入弹窗；按钮文案为“增加”“修改”“润色”，可多字段选择、移除 tag、生成、编辑候选、取消、确认覆盖。
16. 模块和游戏的“汇总”按钮位于同一“AI辅助编辑”分组框中，使用填充色按钮和同一对比确认流程，未确认前不覆盖字段。
17. API 未配置或配置失败时，点击 AI 功能显示不可用或错误提示，不覆盖节点字段。
18. 当前用户不是创建者/上传者时，节点或图片删除按钮不可用；创建者/上传者删除时出现应用内确认弹窗。
19. 删除模块会删除对应模块文件夹、子内容节点和对应 Markdown，但保留图片资产；删除图片会显示引用列表并清理关联和内容 `@` 引用。
20. 删除模块、内容节点或图片后，`manifest.yml`、`INDEX.md`、`image_catalog.yml` 自动更新，左侧索引文件列表保持同步。
21. 左侧右键“粘贴图片”进入上传弹窗；“在文件夹中打开”只打开当前工作区内已知节点、图片或索引文件。
22. 左下角导出 AGENTS/CLAUDE 后，根目录出现或更新 `AGENTS.md`、`CLAUDE.md`，只在左下角导出区显示导出反馈，约 2 秒后自动消失；右下角不出现额外导出完成提示。
23. 左下角导出当前目录后，根目录出现或更新 `manifest.yml`，主节点目录出现或更新 `INDEX.md` 和 `image_catalog.yml`，只在左下角导出区显示导出反馈，约 2 秒后自动消失；右下角不出现额外导出完成提示。
24. 导出的 `AGENTS.md`、`CLAUDE.md`、`manifest.yml`、`INDEX.md`、`image_catalog.yml` 和示例工作区文件中不包含 API key。
25. 设置弹窗顶部只显示一个“设置”，右上角为关闭图标；退出账号按钮位于 API 配置下方，按钮上方显示当前登录用户。
26. 游戏、模块、内容节点详情中的删除和保存按钮均显示为“删除”“保存”，不再带节点类型后缀。
27. AI 辅助编辑弹窗右上角为关闭图标，不显示文本“关闭”按钮。
28. 右侧详情内容很长时，左下角导出 AGENTS/CLAUDE、导出当前目录和设置按钮仍固定可见。
29. 默认中文启动时窗口标题和左上角产品名显示“游戏上下文管理器”；切换英文后显示 `Game Context Manager`；顶部系统菜单栏不显示 File/Edit/View/Window/Help。
30. 左下角设置按钮文字右侧显示齿轮图标。
31. 游戏、模块、内容详情底部程序生成信息框不显示“结构字段”标题，创建者和最后编辑者显示本地用户名。
32. 图片详情页底部不显示原始文件名或生成路径，显示该图片关联的节点；未关联时显示无引用提示。
33. 图片详情页删除按钮显示为“删除”，不再显示“删除图片”。
34. 点击左侧 `AGENTS.md`、`CLAUDE.md`、`manifest.yml`、`INDEX.md` 或 `image_catalog.yml` 时，右侧详情只显示文件内容预览，不显示文件路径栏位；文件缺失时显示读取失败提示。
35. 模块节点和内容节点详情中的“乐趣点”“主要问题”“已知问题”“优化方向”不显示“（主观）”括注；AI 辅助编辑弹窗和导出的 Markdown 标题也不显示该括注。
36. 游戏、模块、内容节点详情中的空输入框不再显示“可留空，MD 中会保留标题方便后续补充。”占位文案。
37. 内容节点详情底部程序生成信息中，模块 ID 右侧显示“所属模块”和父模块名称。
38. 手动编辑任一节点详情字段后保存按钮启用；点击保存或按 `Ctrl+S` 后会写入节点、自动导出目录索引，并显示约 2 秒成功或失败提示。
39. 使用 `corepack pnpm start` 前若刚修改过源码，需要先执行 `corepack pnpm build`；直接开发调试可用 `corepack pnpm dev`。
40. 删除模块或内容节点时，确认弹窗不再提示手动导出目录；确认删除后目录索引自动更新。
41. 左侧已有模块节点的 `+` 右侧显示折叠按钮；有内容子节点时可折叠/展开内容列表，无内容子节点时按钮灰色不可点。
42. 左侧图片库的 `+` 右侧显示折叠按钮；有图片时可折叠/展开图片列表，无图片时按钮灰色不可点。
43. 左侧 Agent 文件和索引文件分组右侧显示折叠按钮；有文件时可折叠/展开文件列表。
44. 模块和内容详情的关联截图多图横向排列，底部横向滚动条可拖拽查看右侧图片，不再把详情页纵向撑得过长。
45. 左侧已有主节点的创建模块 `+` 右侧显示折叠按钮；点击后只折叠/展开模块节点、内容节点和图片库，不折叠 Agent 文件分组或索引文件分组。
46. 左侧树中所有创建子节点的 `+` 使用青色填充白色图标，折叠按钮保持浅色边框；折叠后的 `+` 与创建 `+` 有明显区分。
47. 当前选中主节点、模块节点或其他详情时，点击任意可见内容节点都能直接切到该内容详情，并自动选中它所属模块。
48. 设置为中文时，自动或手动导出的 `AGENTS.md`、`CLAUDE.md`、`INDEX.md`、`manifest.yml` 和 `image_catalog.yml` 中的人类可读内容使用中文；设置为英文时使用英文。文件名、路径、YAML key 和关系 ID 保持英文。
49. 创建主节点、模块节点、内容节点弹窗打开后，在焦点不位于输入框、文本域、下拉框或按钮内时按空格键，会触发对应创建操作；在输入框中按空格仍能正常输入空格。
50. 上传图片弹窗打开后，在焦点不位于输入框、文本域、下拉框或按钮内时按空格键，会触发上传操作；在图片名和备注输入框中按空格仍能正常输入空格。
51. 已打开工作区时点击导入，取消选择文件夹或导入失败后仍停留在原工作区；只有选择有效文件夹并导入成功后才切换工作区。
52. 模块和内容详情中关联多张截图时，截图卡片不会撑出详情页边框；横向滚动条出现在“关联截图”栏位内部，拖动时只改变该栏位内图片的左右显示位置，不触发主界面横向滚动。
53. 模块节点详情中，“关联截图”栏位显示在“AI辅助编辑”分组框下方、模块定位字段上方。
54. 游戏主节点详情不再显示“补充说明”栏位；保存和导出的 `game.md` 不再包含“## 补充说明”段落；导入旧 `game.md` 时即使存在该段落也不再作为游戏字段读取。
55. 拉宽主窗口或最大化后，右侧详情页、文本填充框和关联截图栏位随可用宽度扩展，不再在两侧留下大面积空白。
56. 选中 `AGENTS.md`、`CLAUDE.md`、`manifest.yml`、`INDEX.md` 或 `image_catalog.yml` 后，右侧详情右上角显示“删除”按钮；点击后弹出确认删除弹窗，确认后删除该文件，并自动同步目录索引；删除 AGENT 文件后可通过左下角导出 AGENTS/CLAUDE 恢复。
57. 鼠标位于任意文件、图片或节点条目上右键时，右键菜单最下方显示“删除”；点击后进入与对应详情页删除按钮相同的确认弹窗和删除流程。
58. 在左侧空白处右键时，“删除”选项不可用，避免误删当前详情页条目。
59. 打开设置后，API 配置下方、当前用户上方显示“帮助”按钮；点击后弹窗显示根目录 `README.md` 内容，关闭后返回设置弹窗。
60. 创建模块弹窗示例为“例如：战斗系统”，创建内容弹窗示例为“例如：创角第一天主线关卡进度”，上传图片弹窗示例为“例如：游戏主界面”；这三个弹窗左上角不显示冗余分类字，上传图片右上角不显示重复取消按钮。
61. 新建游戏主节点后，主节点目录名为“游戏名 + 游戏上下文”，例如 `使命防线游戏上下文/`；创建成功后自动生成 `AGENTS.md`、`CLAUDE.md`、`manifest.yml`、`INDEX.md` 和 `image_catalog.yml`，上传图片、保存节点和自动/手动导出的目录索引均使用该目录。
62. 删除游戏主节点后再次创建主节点时，必须重新选择工作区根目录；选择取消或创建失败时仍停留在当前已打开工作区。所选根目录下生成 `.game-context-manager.yml`，后续导出的 `AGENTS.md` / `CLAUDE.md` / `manifest.yml` 与主节点游戏上下文子文件夹同级。
63. 删除游戏主节点后，系统资源管理器中对应的 `<游戏名>游戏上下文/` 文件夹被删除，左侧树不再显示该主节点、模块、内容和图片资产。
64. 在系统资源管理器中手动删除、增加或修改当前工作区内的节点 Markdown、图片资源或索引文件后，点击右侧顶部刷新按钮，左侧目录和右侧详情状态以磁盘重新扫描结果为准。
65. 关闭并重新启动应用后，最近工作区会自动按磁盘内容重新扫描；如果资源管理器中已经删除或修改了节点、图片或索引文件，左侧目录以重启后的扫描结果为准。
66. 在模块详情页或内容详情页点击“删除”时，确认后可正常进入对应删除 IPC，不再出现 `Provided value cannot be bound to SQLite parameter 2` 错误。
67. 点击左下角导出后，导出成功或失败提示显示在导出按钮上方；导出按钮和设置按钮位置保持不变。
68. 已导出 `manifest.yml`、`INDEX.md` 和 `image_catalog.yml` 后，点击刷新或重启恢复工作区，左侧索引文件列表仍显示这三个文件，且三者同级显示。
69. 已导出 `manifest.yml`、`INDEX.md` 和 `image_catalog.yml` 后，在资源管理器中手动删除某个模块文件夹但不重新导出目录索引，点击刷新不应报 `Referenced file is missing`；左侧模块/内容以现存文件为准，目录索引提示需要重新导出。
70. 删除游戏主节点后，系统资源管理器中对应的 `<游戏名>游戏上下文/` 文件夹和工作区根目录 `.game-context-manager.yml` 均被删除；关闭并重启应用后，不应自动恢复该已删除 marker 的工作区。
71. 在游戏、模块、内容详情页焦点位于输入框或文本域时按 `Ctrl+S`，若有改动，应保存当前节点；若没有改动，应不显示保存反馈且不触发浏览器保存页面行为。
72. 创建游戏主节点后，无需点击左下角导出，工作区根目录应出现 `AGENTS.md`、`CLAUDE.md`、`manifest.yml`，主节点目录应出现 `INDEX.md`、`image_catalog.yml`。
73. 保存游戏、模块或内容节点后，`manifest.yml`、`INDEX.md`、`image_catalog.yml` 的更新时间和内容应反映最新字段或节点列表。
74. 上传或删除图片后，`image_catalog.yml` 和 `manifest.yml` 图片条目应自动更新，相关节点 Markdown 中的图片关联也保持一致。

## 9. 已知问题与风险

1. `pnpm lint` 尚未配置。
2. SQLite 使用 Electron/Node `node:sqlite`，运行时会输出 ExperimentalWarning。
3. 当前仍不做回收站、撤销或历史恢复。
4. 节点顺序 ID 当前仍依赖全库唯一主键；测试中避免在同一数据库多工作区创建重复 `game_001`。后续如支持多工作区并行创建同类型节点，需评估 ID 生成范围或数据库唯一约束。
5. 完整 GUI 人工点击烟测需要在可交互桌面窗口中执行；本次仅完成命令级检查和烟测清单记录。

## 10. 下一步指令

后续继续开发时，请让 Codex：

1. 用 UTF-8 完整读取 `CURRENT_STATE.md`。
2. 读取 `TASK.md`；当前没有剩余 TODO，应先由用户新增或指定新任务。
3. 若新增任务涉及产品边界、工作区结构、文案、导出文件或测试规范，读取 `DESIGN.md` 和 `AGENTS.md` 相关章节。
4. 只实现用户指定或 `TASK.md` 中第一个 TODO 任务。
5. 完成后运行合适测试，并回写 `TASK.md` 与 `CURRENT_STATE.md`。
